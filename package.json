{
  "name": "thridprototype",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "15.5.1"
  },
  "main": "app.js",
  "scripts": {
    "start": "http-server src/data --cors -p process.env.PORT & http-server --cors -p process.env.PORT & open http://process.env.PORT/src/views/index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "stop": "lsof -P | grep ':7077' | awk '{print $2}' | xargs kill -9 & lsof -P | grep ':7078' | awk '{print $2}' | xargs kill -9 & killall node"
  },
  "author": "Au Tsz Kin",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.5.3",
    "d3": "^6.2.0",
    "d3-dag": "^0.4.7",
    "jquery": "^3.5.1"
  }
}
